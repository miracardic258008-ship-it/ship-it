services:
  - type: web
    name: radarnav
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3001
      - key: RENDER_URL
        fromService:
          type: web
          name: radarnav
          property: host
